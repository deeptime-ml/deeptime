package:
  name: deeptime
  version: 0

source:
  git_url: ../..

build:
  script:
    - "{{ PYTHON }} -m pip install . -vvv"

requirements:
  build:
    - {{ compiler('c') }}
    - {{ compiler('cxx') }}

  host:
    - python
    - cython
    - pip
    - scipy
    - toml
    - ninja
    - cmake >=3.22
    - numpy >=1.14
    - pybind11 >=2.9.0
    - scikit-build >=0.13
    - llvm-openmp  # [osx]
    - libgomp  # [linux]

  run:
    - python
    - {{ pin_compatible('numpy') }}
    - scipy
    - scikit-learn
    - threadpoolctl

test:
  imports:
    - deeptime
