[build-system]
requires = [
    "setuptools",
    "wheel",
    "numpy>=1.14",
    "Cython",
    "scipy",
    "pybind11",
]

[tool.pytest.ini_options]
filterwarnings = [
    "once",
    "ignore::UserWarning",
]
